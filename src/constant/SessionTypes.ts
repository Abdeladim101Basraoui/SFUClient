export enum TPeer {
    PRODUCER = 'producer',
    CONSUMER = 'consumer',
    DATA = 'data',
}

export enum TState {
    CONNECTING = 'connecting',
    CONNECTED = 'connected',
    FAILED = 'failed',
    CLOSED = 'closed',
}

    
